# backend/requirements.txt
# Auto-D Kenya API Dependencies

# ─── Core Framework ──────────────────────────────────────────────
# Use versions with pre-built wheels for faster installation
fastapi==0.104.1
uvicorn[standard]==0.24.0

# ─── Data Validation ─────────────────────────────────────────────
# Use pydantic without building from source
pydantic==2.4.2  # Older version with better wheel support
pydantic-settings==2.1.0

# ─── Supabase ─────────────────────────────────────────────────────
# Let supabase manage its own compatible sub-dependency versions
supabase==2.5.3

# ─── Authentication ──────────────────────────────────────────────
# Pure Python auth libraries
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
email-validator==2.1.0

# ─── HTTP Clients ────────────────────────────────────────────────
# Pure Python HTTP libraries
httpx==0.27.2
aiohttp==3.9.1
requests==2.31.0  # <-- ADDED: Required for M-Pesa API calls

# ─── Environment ─────────────────────────────────────────────────
# Pure Python environment management
python-dotenv==1.0.0

# ─── Production Server ───────────────────────────────────────────
# Pure Python production server
gunicorn==21.2.0

# ─── Utilities ───────────────────────────────────────────────────
# General utilities
python-dateutil==2.8.2
tenacity==8.2.3

# ─── Optional: For better M-Pesa integration ────────────────────
# If you need retry logic for M-Pesa API calls
# backoff==2.2.1
# pyyaml==6.0.1
